---
import { playerDataService } from '../services/playerdata/playerdata.service.ts';
const x = await playerDataService.getAllPlayerData();
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<link rel="icon" type="image/x-icon" href="/favicon.ico" />
		<title>Demo</title>
	</head>
	<body>
		<h1>Test app</h1>
        <h2>season {x.seasons[0].year}</h2>
       {JSON.stringify(x.seasons[0])}
        <h2>season {x.seasons[1].year}</h2>
       {JSON.stringify(x.seasons[1])}
        <h2>season {x.seasons[2].year}</h2>
       {JSON.stringify(x.seasons[2])}
	</body>
</html>
